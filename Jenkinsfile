Pipeline {
  agent any
  stages {
    stage("build") {
      step {
        echo "Building the pipeline"
      }
    }
     stage("staging") {
      step {
        echo "staging the pipeline"
      }
    }
     stage("prod") {
      step {
        echo "prod the pipeline"
      }
    }
  }
}
